import { FluentWindow } from "./components/window.js";
import { FluentDialog } from "./components/dialog.js";

const VERSION = "1.0.0"
const BUILDVERSION = "10H1"

console.log(`%c FluentWeb.js v${VERSION} (${BUILDVERSION})`,"background: #0078d4; color: #fff; border-radius: 4px;");

export { FluentWindow, FluentDialog };